-Some more export logic is need.
-More types for answers are need
-Trying to make DB insert after test make.
-Moved from MySQL to PostgreSQL because MySQL python module won't be compiled.
-Now program generates test and writes to DB.
